# internal: cdxgen-packages

This internal repo serves as hub for sycing/publishing pachages for `cdxgen`.

## Background

The `cdxen` repos were moved to a dedicated org `cdxgen`: <https://github.com/cdxgen>  
Som packages are still published in the CycloneDX org: <https://github.com/orgs/CycloneDX/packages>  

To make it possible to update the exising packages, this repo playes a critical role. 
